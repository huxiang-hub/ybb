<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anaysis.mysqlmapper.ProdProcelinkMapper">

    <select id="selectByPtIdAndPrId" resultType="com.anaysis.entity.ProdProcelink">
        SELECT * FROM yb_prod_procelink WHERE pt_id = #{ptId} AND pr_id = #{prId}
    </select>

</mapper>
